import 'package:flutter/material.dart';
import '../widgets/my_board.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: MyBoard(),
    );
  }
}
